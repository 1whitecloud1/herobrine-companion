# Mod Properties
mod_id=herobrine_companion
mod_name=Herobrine Companion
mod_license=GPL-3.0
mod_version=0.34
mod_group_id=com.whitecloud233.modid.herobrine_companion
mod_authors=WhiteCloud233


# Minecraft & Forge
minecraft_version=1.20.1
minecraft_version_range=[1.20.1,1.21)
forge_version=47.4.16
forge_version_range=[47,)
loader_version_range=[47,)
mapping_channel=official
mapping_version=1.20.1

# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
# The description of the mod. This is a simple multiline text string that is used for display purposes in the mod list.
mod_description=
# JEI Version
# ????? HTTP ??
#systemProp.http.proxyHost=127.0.0.1
#systemProp.http.proxyPort=7897
#systemProp.https.proxyHost=127.0.0.1
#systemProp.https.proxyPort=7897
org.gradle.jvmargs=-Dnet.minecraftforge.gradle.check.certs=false
org.gradle.java.home=D:/jdk-17.0.15
# Keep Epic Fight compatibility opt-in per invocation.
# Examples:
#   gradlew build -PenableEpicFightCompat=true
#   gradlew runClient -PenableEpicFightCompat=false

# Kaleidoscope Cookery runtime version: default 1.4.1 (8266769).
# Switch to 1.3.0 to verify dual-version compatibility:
#   gradlew runClient -PcookeryFileId=7966280

# Build flavors: the DEFAULT build is the safe build, excluding local computer
# control (cmd execution), JVM code injection and web lookup features.
# Full build with all three features:
#   gradlew build -Pfull=true   (jar file name gets a -full suffix)
# Legacy explicit safe flag still works:
#   gradlew build -PcfSafe=true
